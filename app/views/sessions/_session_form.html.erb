<%= form_for(:session, url: sessions_path) do |s| %>
	<p>
		<%= s.label :email %><br>
		<%= s.email_field :email %>
	</p>

	<p>
		<%= s.label :password %><br>
		<%= s.password_field :password %>
	</p>

	<p>
		<%= s.submit "Login" %>
	</p>
<% end %>
<%= link_to("Sign up", new_user_path ) %>